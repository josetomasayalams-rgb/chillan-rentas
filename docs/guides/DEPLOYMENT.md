# Despliegue

Publica la carpeta completa como sitio estático, incluidos los recursos de `assets/` y el manifiesto. Antes de publicar, actualiza la versión de recursos cuando corresponda, ejecuta `make ci` y comprueba el flujo operativo en un navegador. El camino recomendado de protección de acceso está documentado en `README.md`.
